# DMAS-Calculator
A lightweight Python DMAS calculator that parses and evaluates multi-number math expressions by strictly enforcing standard arithmetic precedence. Built using a flat, linear approach with minimal abstraction, it directly processes raw string inputs to execute sequential division, multiplication, addition, and subtraction.
